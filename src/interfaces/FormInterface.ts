export interface LoginFormTypes {
    email: string;
    password: string;
}

export interface RegisterFormTypes {
    email: string;
    password: string;
    username: string
}
export interface PostFormTypes {
    email: string;
    password: string;
    username: string
}